# 메신저 RAG - Streamlit 데모 앱 의존성
#
#   pip install -r requirements-app.txt
#   streamlit run app.py
#
# UI 껍데기만 돌릴 때는 이 파일이면 충분하다. 청크 JSON 을 직접 읽어
# 화면을 채우므로 임베딩 모델 없이도 흐름을 확인할 수 있다.
#
# 실제 검색(임베딩 .npz 로드 + 질의 인코딩)을 붙일 때는 임베딩 모델이 필요하다:
#   pip install -r requirements.txt
# 그때 transformers 버전 제약(4.51.x)이 그대로 적용되니 requirements.txt 상단 주석을 볼 것.

streamlit>=1.40
numpy>=1.24

# 질의 재작성/확장 (src/query_rewrite.py) — Gemini Flash
# 키는 .env 의 GEMINI_API_KEY 에서 읽는다. .env 는 .gitignore 로 제외돼 있다.
google-genai>=1.0
python-dotenv>=1.0
