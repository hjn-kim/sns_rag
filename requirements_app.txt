# 메신저 RAG - Streamlit 데모 앱 의존성
#
#   pip install torch --index-url https://download.pytorch.org/whl/cpu   # 먼저!
#   pip install -r requirements-app.txt
#   streamlit run app.py
#
# torch 를 왜 따로 먼저 까나:
#   그냥 `pip install torch` 하면 CUDA 빌드가 온다. nvidia-* 의존성까지 2.5~3GB 인데
#   GPU 가 없으면 한 줄도 안 쓰인다. 위 CPU 인덱스를 쓰면 200MB 로 끝난다.
#   GPU 팟(RunPod 등)에서 돌릴 때는 반대로 드라이버에 맞는 CUDA 빌드를 깔아야 한다.
#   requirements_runpod.txt 상단 주석 참고 (cu128 등).
#
# 모델은 첫 검색 때 HuggingFace 에서 자동으로 받는다(약 1.2GB, ~/.cache/huggingface).
# 메모리는 모델 2.4GB + 나머지 0.7GB 정도라 여유 4GB 는 있어야 한다.

# --- 화면 -----------------------------------------------------------------
streamlit>=1.40
numpy>=1.24

# --- 1,2 질의 재작성/확장 · 4 리랭킹 · 6 답변 생성 (src/multi_query.py,
#     src/rerank.py, src/answer.py) — Gemini Flash-Lite
# 키는 .env 의 GEMINI_API_KEY 에서 읽는다. .env 는 .gitignore 로 제외돼 있다.
google-genai>=1.0
python-dotenv>=1.0

# --- 3 질의 임베딩 + 청크 검색 (src/search.py, src/comparison.py) ----------
# 문서 임베딩(data/emb/*.npz)을 만든 것과 같은 모델이어야 한다.
#   microsoft/harrier-oss-v1-0.6b, 1024차원
# 모델을 바꾸면 벡터 공간이 달라져 코사인 유사도가 무의미해진다.
sentence-transformers>=5.0
transformers>=4.56
