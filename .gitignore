# API 키. 절대 커밋하지 말 것.
.env
.env.*
!.env.example

# 파이썬
__pycache__/
*.py[cod]
.venv/
venv/

# 임베딩 결과 (embedding.py 가 만든다. 원본에서 언제든 다시 만들 수 있다)
data/emb/

# 번역 캐시
data/.translate_cache/

# 임시 파일
*.tmp
